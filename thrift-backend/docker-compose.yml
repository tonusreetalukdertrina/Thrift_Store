services:
    db:
        image: postgres:16-alpine
        restart: unless-stopped
        environment:
            POSTGRES_DB: thrift_store
            POSTGRES_USER: thrift
            POSTGRES_PASSWORD: thrift_secret
        ports:
            - "5437:5432"
        volumes:
            - pgdata:/var/lib/postgresql/data

volumes:
    pgdata:
